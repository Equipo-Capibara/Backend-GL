spring.application.name=bakend-GL
spring.data.mongodb.database=GdlL
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/}

# Configuración de Redis
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}

# Configuración de CORS
cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000}

# Configuración de WebSocket
websocket.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000}
websocket.endpoint=/ws
websocket.broker-destinations=/topic
websocket.application-prefixes=/app

# Configuración del servidor
server.port=${PORT:8080}

